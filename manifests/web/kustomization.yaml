apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: deepdoc
resources:
  - deployment.yaml
  - service.yaml
# Rewritten by the deepdoc_client CI `bump` job. A different file from the
# API's, which is why the two pipelines can never race.
images:
  - name: ghcr.io/roongchakkrin42/deepdoc-web
    newTag: latest
